# State Decisions

## 1. useState
Use useState for simple state inside one component, like form inputs or modal visibility.

## 2. Context + useReducer
Use Context + useReducer for small global state, like theme or logged-in user.

## 3. TanStack Query
Use TanStack Query for server data because it handles fetching, caching, and updating automatically.