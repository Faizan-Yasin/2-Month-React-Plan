// const name = "Faizan"

// console.log(name ?? "Guest");

// const name = null

// console.log(name ?? "Guest");

// const name = undefined

// console.log(name ?? "Guest");

// const name = undefined

// console.log(name || "Guest");

// const name = null

// console.log(name || "Guest");

// const name = 0

// console.log(name || "Guest");

const name = ""

console.log(name ?? "Guest");

// ?? uses the fallback only when the value is null or undefined.
// || uses the fallback for any falsy value like 0, false, or "".
