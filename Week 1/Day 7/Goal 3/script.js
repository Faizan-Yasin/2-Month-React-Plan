// const arr = [1, 2, 3];

// const result = arr.map((item) => {
//     return item * 2
// })

// console.log(result)

const arr = [1, 2, 3];

const result =
    arr.map(
        item => item * 2
    );

console.log(result);

// const student = "Name : " + name + " Age : " + age

const student = `Name: ${name} Age: ${age}`;
